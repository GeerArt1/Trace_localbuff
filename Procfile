# ══════════════════════════════════════════════
# TRACE — Procfile (Railway / Heroku / Fly.io)
# ══════════════════════════════════════════════
# Railway uses 'npm start' by default, but this
# Procfile gives explicit control.
# ══════════════════════════════════════════════

web: node trace_server.js

# For cluster mode (multi-core), use instead:
# web: node trace_cluster.js
